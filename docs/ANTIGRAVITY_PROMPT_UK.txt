У корені мого локального Git-проєкту MyKep є mykep-navbar-rollback-2.6.8.zip. Застосуй цільовий відкат мобільних змін 2.6.7 і запусти локально. НЕ роби commit/push/deploy до мого окремого підтвердження після перевірки.

1. Покажи поточну гілку й git status --short. Не роби reset/clean/stash і не перезаписуй мої нові правки. Не виводь .env чи секрети.
2. Перевір ZIP і розпакуй у тимчасову папку поза репозиторієм. Відхили абсолютні шляхи, ../, drive/UNC paths і symlink entries. Усі розпаковані файли мають лишатися в тимчасовій папці.
3. Прочитай mykep-main/UPDATE_MANIFEST.json і docs/NAVBAR_ROLLBACK_2.6.8_UK.md. База оновлення - наданий раніше ZIP 2.6.7. НЕ застосовуй історичні промпти 2.6.6/2.6.7.
4. ДО зміни локального проєкту перевір updated_sha256 усіх entries-файлів архіву й локальні хеші. Локальний файл із before_sha256 можна оновити; файл із updated_sha256 вже оновлений, пропусти. Будь-яка інша версія або відсутній файл із ненульовим before_sha256 - конфлікт: зупинись і спитай про злиття. Новий файл із before_sha256=null має бути відсутній або вже мати updated_sha256. Не перезаписуй новіший код. Перевір, що локальні цільові файли та батьківські папки не ведуть через symlink за межі проєкту.
5. Зроби резервну копію зачеплених локальних файлів поза репозиторієм, збережи список нових файлів. Копіюй ТІЛЬКИ entries, нічого не видаляй. UPDATE_MANIFEST.json використай зовні, він сам не входить до entries. Не чіпай .git, .env, БД, секрети та середовища. Після копіювання перевір хеші й покажи diff.
6. У цьому релізі iOS CSS повернуто точно до 2.6.6: height:100lvh, nav position:absolute/bottom:0, резерв місця та safe-area. display-mode.js більше не стискає iOS-shell через visualViewport. Zoom-обмеження viewport/CSS/JS повернуті на прохання користувача. Попередження SEO-розширення про zoom прийнятні - не виправляй їх знову. НЕ міняй H1/H2, аватари, SEO-описи, короткі тире, Android-компонування, дані викладачів і backend. Не опускай nav магічним negative bottom або приховуванням safe-area. Це відкат, а не нове доведене виправлення початкового Firefox/iOS сценарію.
7. Підготуй venv і requirements-dev.txt. Для тестів і локального сервера задай BOT_MODE=disabled та MYKEP_DB_PATH як абсолютний шлях до окремої тестової БД поза репозиторієм. Не редагуй production .env і не запускай бота.
Виконай python -m unittest discover -s tests -v, node --test tests/*.test.cjs, node tests/test_seo_client.cjs і node --check для кожного static/js/*.js та static/sw.js. SKIP/помилки імпорту не рахуй як PASS. У підготовчому середовищі пройшли 52 JS-тести, 16 статичних Python-перевірок, client-check та синтаксис. Повні HTTP/teacher Python-тести та реальний браузер там не виконані.
8. Запусти python -m uvicorn main:app --host 127.0.0.1 --port 8000 з env вище. Покажи URL і залиш сервер для перевірки. Не відкривай публічний tunnel і не роби deploy. Перевір головну, налаштування, розклад, модальні вікна та онбординг. У налаштуваннях і діагностиці має бути 2.6.8; SW-кеш - mykep-cache-v2.6.8. Не очищай дані production PWA.
9. Особлива перевірка: на тому iPhone, де у 2.6.7 під навбаром великий порожній відступ, перевір запуск іконкою, налаштування, головну, розклад, rotation і повернення після згортання. Збережи safe-area під кнопками, не допускай перекриття домашнього індикатора. Додатково перевір Firefox iOS -> Home Screen: відкат повертає попередню геометрію, тому початковий Firefox-баг може відтворитися. Desktop emulation не доводить роботу на iPhone. Якщо пристрою немає, попроси мене перевірити; не оголошуй баг підтверджено виправленим.
Для тесту на телефоні спочатку погодь безпечний локальний HTTPS-доступ. Якщо nav досі не на місці, попроси модель iPhone, версію iOS, спосіб встановлення та результат window.mykepViewportReport(). Не додавай нових CSS-зсувів навмання.
10. Покажи diff, результати PASS/SKIP/FAIL, обмеження, локальний URL і як зупинити сервер. ЗУПИНИСЬ для мого перегляду.

ЗАРАЗ ЗАБОРОНЕНО commit, push, PR, deploy, force push, git add . та git add -A. Лише після мого окремого явного підтвердження після локальної перевірки погодь гілку/remote, додай конкретні узгоджені файли, перевір staged diff і виконай commit/push. Не додавай ZIP, тестові БД, бекапи, секрети, venv чи сторонні зміни.
